{% block body %}

	<h1>
		{% block title %}
			{{post.title}}
		{% endblock %}
	</h1>

	<p>{{post.body}}</p>

	<h3>Nombre de likes :
		{{post.nbLikes}}</h3>
	<h4>Créé le
		{{post.createdAt.format('d-m-Y')}}</h4>
{% endblock %}
